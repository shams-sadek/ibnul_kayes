// import {
//     SET_NAME,
// } from '../actions/userActions'

const initialState = {
    fName: 'Sabbir',
    lName: 'Ahmed',

}

export const sabbirReducer = (state = initialState, action) => {
    switch (action.type) {
        // case SET_AGE:
        //     state = {
        //         ...state,
        //         age: action.payload
        //     }
        //     break;
    }

    return state
}

